.claude.md
CLAUDE.md

# Build artifacts
live-iso-work/

# Docs build artifacts
docs/node_modules/
docs/.nuxt/
docs/.output/
docs/dist/

# Local agent/AI scratch dirs
.claude/

# VM debugging artifacts (never commit these)
guest_*.txt
guest_*.log
TelcoChisel-live.iso
TelcoChisel-live_*.iso
PRESS_KIT.md
outreach.md

# Secrets — never commit private keys (e.g. the SourceForge deploy key)
sf_deploy
sf_deploy.pub
*.pem
id_rsa
id_ed25519
*_rsa
*_ed25519
